[build]
  command = "npm run build"
  publish = ".next"

# 正确写法1：完全禁用 functions
[functions]
  "*" = false

# 或者正确写法2：指定空目录
# [functions]
#   directory = ""

[build.environment]
  NEXT_DISABLE_EDGE = "true"
  NEXT_PUBLIC_USE_EDGE = "false"