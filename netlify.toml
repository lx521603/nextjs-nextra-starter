[build]
  command = "npm run build"
  publish = ".next"

# 完全禁用 functions 和 edge functions
[functions]
  "*" = false
  directory = null

[build.environment]
  NEXT_DISABLE_EDGE = "true"
  NEXT_PUBLIC_USE_EDGE = "false"
